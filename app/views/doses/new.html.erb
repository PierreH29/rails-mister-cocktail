<h1>Create dose for <%= @cocktail.name %></h1>


<%= simple_form_for([@cocktail, @dose]) do |f| %>
  <%= f.input :description %>
  <%= f.association :ingredient %>
 <!-- <%= f.input :ingredient_id, collection: @ingredients %> -->
 <!-- # pour remplir la liste des ingredients -->
  <%= f.button :submit %>
<% end %>
